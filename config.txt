WIDTH=32
HEIGHT=32
ENTRY=1,1
EXIT=30,30
OUTPUT_FILE=maze.txt
PERFECT=False
SEED=42
ALGORITHM=dfs
# WIDTH Maze width (number of cells)        example: WIDTH=32   max for multisolution is 32x32 area
# HEIGHT Maze height                        example: HEIGHT=32
# ENTRY Entry coordinates (x,y)             example: ENTRY=0,0
# EXIT Exit coordinates (x,y)               example: EXIT=30,30
# OUTPUT_FILE Output filename               example: OUTPUT_FILE=maze.txt
# Is the maze perfect?                      example: PERFECT=False
# SEED To generate the maze                 example: SEED=42
# ALGORITHM The alrgorithm gen the maze     example: ALGORITHM=dfs